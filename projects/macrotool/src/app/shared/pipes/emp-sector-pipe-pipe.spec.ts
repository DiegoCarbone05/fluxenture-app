import { EmpSectorPipePipe } from './emp-sector-pipe-pipe';

describe('EmpSectorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmpSectorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
