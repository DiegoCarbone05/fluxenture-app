import { AbstentTypePipe } from './abstent-type-pipe';

describe('AbstentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AbstentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
